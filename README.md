# Getting Started
First, clone git repository on terminal
```bash
git clone https://github.com/novaandini/wastecare.git
cd wastecare
```
Then, copy config.properties.example to config.properties
```bash
cd src/config
cp config.properties.example config.properties
```
Finally run project with laragon
